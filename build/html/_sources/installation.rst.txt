Installation
============

To install DeepReaction, make sure you have Python 3.7 or above. You can install the dependencies using pip:

.. code-block:: bash

   git clone https://github.com/yourusername/deepreaction.git
   cd deepreaction
   pip install -r requirements.txt

If you want to install in development mode:

.. code-block:: bash

   pip install -e .
